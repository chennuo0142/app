<nav class="navbar navbar-expand-lg bg-light container nav-grid">
	<div class="container-fluid">
		<div>
			<a class="navbar-brand" href="{{ path('app_home') }}">Paris Prestige Transfert</a>
		</div>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNavDropdown">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{path('app_home')}}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('app_contact')}}">Contact</a>
				</li>
				{% if app.user %}


					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							<img src="/image/ico/utilisateur.svg" width="20" alt="">
							{{ app.user.name }}(id:{{ app.user.id }})
						</a>


						<ul class="dropdown-menu">
							<li>
								<a class="dropdown-item" href="{{ path('app_profile_index') }}">Profile</a>
							</li>
							{% if app.user.isCompany %}
								<li>
									<a class="dropdown-item" href="{{ path('app_gestion_index') }}">Gestion</a>
								</li>
							{% endif %}
							{% if is_granted('ROLE_ADMIN') %}
								<li class="nav-item">
									<a class="dropdown-item" href="{{ path('app_admin') }}">Admin</a>
								</li>
							{% endif %}
							<li class="nav-item">
								<a class="dropdown-item" href="{{ path('app_setting') }}">Parametre</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{path('app_logout')}}">Deconnexion</a>
							</li>
						</ul>
					</li>
					<button class="btn btn-primary" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasExample" aria-controls="offcanvasExample">
						Gestion
					</button>

				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{path('app_login')}}">Connexion</a>
					</li>

				{% endif %}


			</ul>
		</div>
	</div>
</nav>
