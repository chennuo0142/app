{% extends 'base.gestion.html.twig' %}

{% block title %}Edit User
{% endblock %}

{% block body %}
	<h1>Edit User</h1>
	<div class="mb-1">
		<a href="{{ path('app_admin_users') }}" class="btn btn-primary">Retour List Users</a>
	</div>
	{{ form_start(form) }}
	{{ form_widget(form) }}

	{{ form_end(form) }}


{% endblock %}
